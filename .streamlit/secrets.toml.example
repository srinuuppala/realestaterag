# Copy these into Streamlit Community Cloud:
#   App → Settings → Secrets → paste → Save
# Streamlit exposes them as environment variables, which is exactly what
# src/config.py reads. No code change is needed.

GROQ_API_KEY = "gsk_your_key_here"
LLM_BASE_URL = "https://api.groq.com/openai/v1"
LLM_MODEL = "llama-3.3-70b-versatile"

AUTH_USERNAME = "demo"
AUTH_PASSWORD = "realestate2026"
